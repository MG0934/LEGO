spring:
  application:
    name: lego-management-service
server:
  port: 8093
eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:8761/eureka/
  instance:
    prefer-ip-address: true